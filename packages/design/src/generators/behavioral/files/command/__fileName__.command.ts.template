/**
 * <%= command %>
 */
interface Command {
  execute(): void;
}

/**
 * <%= simpleCommand %>
 */
class <%= className %>Command implements Command {
  private payload: string;

  constructor(payload: string) {
    this.payload = payload;
  }

  execute(): void {
    console.log(
      `SimpleCommand: See, I can do simple things like printing (${this.payload})`
    );
  }
}